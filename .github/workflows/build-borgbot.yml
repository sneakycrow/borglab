name: build borgbot

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/rust-build-and-test.yml
    with:
      package: borgbot
